object HelloWorld {
    def main(args: Array[String]){
        var a: Int = 2
        var b: Int = 3
        var c: Int = 4
        var d: Int = 5
        var k: Float = 4.3f
        
        println("a= " + a)
        println("b= "+ b)
        println("c= "+c)
        println("d= "+d)
        println("k= "+ k)
        
        // and evaluate the expressions
        
        // println( - -b * a + c *d - -);
        // println(a++);
        // println (–2 * ( g – k ) +c);
        // println (c=c++);
        // println (c=++c*a++);

        println("this are a java code (a,b,d,e). thearfor this ocde do not run becouse this is scala")
        println("x++ and ++x and --x and x-- is do not work in scala")
        println("println (- 2 * ( g - k ) +c); g is do not define variable and  '–' is not a '-' ")


    }
}