/* Online Scala Compiler */
object HelloWorld {
   def main(args: Array[String]) {
      var k , i , j = 2
      val m , n = 5
      val f=12.0f
      val g=4.0f
      val c='X'
      
      //a) k + 12 * m
      println(k+12*m)
      // b) m / j
      println(m/j)
      // c) n % j
      println(n % j)
      // d) m / j * j
      println(m / j * j)
      // e) f + 10*5 +g
      println(f + 10*5 +g)
      // f) ++i * n
      // i += 1
      i += 1
      println(i * n)
   }
}